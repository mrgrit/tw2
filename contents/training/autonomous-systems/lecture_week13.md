# autonomous-systems W13 — AI 모델 공격/방어: 적대적 입력·로버스트니스·검증

> **본 주차의 한 줄 요약**
>
> 자율 시스템은 인식·판단을 **AI 모델**에 의존한다(W06·W07). W13은 그 AI 모델 자체의 공격/방어를 **모델
> 수준**에서 심화한다. 핵심 위협은 **적대적 예제(adversarial example)** — 모델이 오분류하도록 입력에 더한 **사람
> 눈엔 안 보이는(또는 자연스러운) 작은 변형**이다. 원리: 신경망은 고차원 결정 경계를 갖는데, 입력을 **경계 너머로
> 미는 최소 변형**을 경사(gradient) 방향으로 계산하면(FGSM·PGD 등) 모델을 속인다. 정지 표지 픽셀에 미세 노이즈
> →"속도 제한", 사람에 패치→"미탐지"(W07 물리판). 자율 시스템에선 이것이 **물리 사고**다. 방어는 모델의
> **로버스트니스(robustness, 강건성)** 를 높이는 것: ① **적대적 학습(adversarial training)** — 적대적 예제를
> 학습 데이터에 넣어 모델을 강하게(가장 효과적), ② **입력 전처리·정제** — 노이즈 제거·랜덤화로 섭동 완화,
> ③ **탐지(detection)** — 적대적 입력의 통계적 이상(비정상 신뢰도·일관성)을 탐지해 거부, ④ **정합성/중복**
> (W06) — 여러 센서·모델로 교차 검증, ⑤ **형식 검증(formal verification)** — 일정 섭동 내에서 출력이 안 바뀜을
> **수학적으로 보장**(안전 필수 영역). 완벽한 방어는 없어 **심층 방어**(강건 모델+탐지+중복+안전 모니터)로 위험을
> 낮춘다. 자율 시스템 AI는 정확도만이 아니라 **강건성**이 안전의 요건이다.
>
> **한 줄 결론**: 적대적 예제는 미세 변형으로 AI 모델을 오분류시켜 자율 시스템에 물리 사고를 낸다. 방어 =
> **적대적 학습 + 입력 정제 + 탐지 + 센서/모델 중복 + 형식 검증**(심층 방어). AI는 정확도만이 아니라 강건성이 요건.

---

## 학습 목표

본 주차 종료 시 학생은 다음 5가지를 **본인 손으로** 할 수 있어야 한다.

1. **적대적 예제**의 원리(결정 경계·경사)를 설명한다.
2. 적대적 섭동으로 **오분류를 유도**한다(ADVERSARIAL_CRAFTED).
3. 모델 **로버스트니스**를 측정한다(ROBUSTNESS_MEASURED).
4. **적대적 학습·탐지·검증**으로 강화한다(MODEL_HARDENED).
5. 왜 강건성이 안전의 요건인지 설명한다.

> **이 주차의 시선** — AI 모델의 근본 취약점(적대적 예제)을 이해하고, 강건성으로 안전을 높인다.

---

## 0. 용어 해설 (AI 모델 보안)

| 용어 | 영문 | 뜻 | 비유 |
|------|------|----|------|
| **적대적 예제** | Adversarial Example | 오분류 유도 입력 | 착시 |
| **결정 경계** | Decision Boundary | 분류 경계 | 판단 선 |
| **로버스트니스** | Robustness | 섭동 강건성 | 흔들림에 강함 |
| **적대적 학습** | Adversarial Training | 적대 예제 학습 | 예방접종 |
| **형식 검증** | Formal Verification | 수학적 보장 | 증명 |

> **헷갈리기 쉬운 한 쌍** — *정확도* 는 "정상 입력에 맞힘", *로버스트니스* 는 "섭동에도 안 틀림"이다. 자율
> 시스템엔 둘 다 필요.

---

## 0.5 신입생 친화 핵심 개념

### 0.5.1 적대적 예제 — 경계를 넘는 최소 변형

```mermaid
graph LR
    X["정상 입력<br/>(정지 표지)"] -->|경사 방향 미세 섭동| X2["적대적 예제<br/>(눈엔 정지)"]
    X2 --> M["모델"]
    M --> W["오분류: 속도 제한"]
    style W fill:#f85149,color:#fff
```

모델의 결정 경계 바로 안쪽 입력을, 경사를 따라 **경계 너머로 미는 최소 변형**을 더하면 오분류한다. 사람 눈엔
거의 안 보이지만 모델은 속는다.

### 0.5.2 공격 방법 (개념)

- **FGSM**: 손실의 경사 부호 방향으로 한 번에 섭동(빠름).
- **PGD**: 여러 번 반복해 더 강한 섭동(강력).
- **물리 패치**(W07): 프린트 가능한 큰 섭동(실세계).
핵심은 **경사 정보**로 모델의 약점을 정확히 찌른다는 것.

### 0.5.3 로버스트니스 측정

모델이 **얼마나 큰 섭동까지 견디는지**(정확도 유지)를 측정한다. 정상 정확도 99%라도 작은 섭동에 10%로 떨어지면
**강건하지 않다**. 자율 시스템 안전엔 **적대적 정확도(robust accuracy)** 가 중요 — 최악의 입력에도 안전한가.

### 0.5.4 방어 — 심층 방어

- **적대적 학습**: 적대적 예제를 학습에 포함해 경계를 강하게(가장 효과적, 비용 큼).
- **입력 전처리**: 노이즈 제거·JPEG 압축·랜덤화로 섭동 완화(부분적).
- **탐지**: 적대적 입력의 이상(비정상 신뢰도 분포·일관성 결여) 탐지·거부.
- **중복·정합성**(W06): 여러 센서·모델 앙상블로 교차 검증(한 모델 속아도 배제).
- **형식 검증**: 안전 필수 영역에서 **일정 섭동 내 출력 불변**을 수학적으로 증명.
어느 하나로 완벽하지 않아 **겹층**으로 위험을 낮춘다.

### 0.5.5 el34 맥락

적대적 예제 실제 생성은 GPU·모델이 필요하다. 본 실습은 **적대적 섭동·로버스트니스·방어 로직**을 결정론 시뮬로
익힌다. 실제 모델 공격/적대적 학습은 GPU 훈련 환경이 필요함을 명시한다.

---

## 1. 실습 안내 (5 미션)

실행 위치 el34 **호스트**(`ssh ccc@{{TARGET_IP}}`), GPU `http://211.170.162.139:10934`.
⚠️ 실제 적대적 학습은 GPU 훈련 필요 → 본 실습은 섭동·강건성·방어 로직 결정론 시뮬.

### STEP 1 — GPU 헬스체크 → GEN_OK
### STEP 2 — 적대적 섭동 오분류 → ADVERSARIAL_CRAFTED
### STEP 3 — 로버스트니스 측정 → ROBUSTNESS_MEASURED
### STEP 4 — 모델 강화 → MODEL_HARDENED
### STEP 5 — 종합 → Assessment

---

## 2. 흔한 오해·관제자 노트

- **"정확도 높으면 안전"** — 섭동에 약할 수 있다. 강건성 별도.
- **"적대적 예제는 이론"** — 물리 패치로 실세계 시연. 자율 시스템 사고.
- **"한 방어면 됨"** — 완벽 방어 없음. 심층 방어.
- **관제 관점** — 자율 AI가 적대적 학습·탐지·중복·검증을 갖췄는지, 강건 정확도가 측정됐는지 점검한다. AI 안전은
  강건성이 요건.

---

## 3. 다음 주차 (W14) 예고 — CPS 인시던트 대응

W13이 "AI 모델 보안"이었다면, W14는 **CPS 인시던트 대응** — 사이버물리 인시던트의 특수성(물리 안전·증거·복구)을
다룬다. 사이버 IR(agent-ir)의 CPS판이다.
