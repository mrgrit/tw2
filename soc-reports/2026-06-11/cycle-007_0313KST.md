# SOC 관제 리포트 — Cycle 007 (Intel: SSRF · 지속성 패턴 · 시스템적 방어 결함)

| 항목 | 값 |
|------|-----|
| 생성 시각 | 2026-06-11 03:13 KST |
| 유형 | **INTEL** — 신규 기법(SSRF) + 백도어 계정 패턴 확정 + 방어 안티패턴 시스템화 |
| 델타 윈도우 | 2026-06-10T17:44:01Z ~ 18:13:37Z |
| 신규 이벤트 | 143 (mission_check 113 / grade 20 / alert 10) |
| last_seen 갱신 | **2026-06-10T18:13:37.481Z** |

> 연습이 SOC관제 W11~W15(C2·UEBA·인텔주도·자율관제) + 공격기법 W6~W10(IDOR·SSRF·CTF·scapy·우회)로
> 또 확장. 위협엔 **SSRF** 가 추가됐고, 백도어 계정 생성이 **반복 패턴**으로 확정됐으며, 방어 룰 결함이
> 개별이 아니라 **시스템적**임이 드러났다.

---

## 1. 위협 측

### INC-003 확장 — SSRF 추가
- 신규 기법 **SSRF**: 미션/룰 증거에서 클라우드 메타데이터 표적 `content:"169.254.169.254"` 확인(scn62).
  기존 SQLi/XSS/LFI/스캐너에 **SSRF** 가 더해짐. 출처 `10.20.30.202`(attacker-ext) 여전히 일관, WAF 차단.
- HAProxy rule 100251 alert 10건(17:51~18:11Z) 지속.

### INC-004/006 일반화 — **백도어 계정 생성 패턴** (개별 INC → 패턴으로 집약)
- 신규 확인: `mw11bot:x:1003` (`useradd -m -s /bin/bash mw11bot`), `insdr12:x:1004`.
- 누적: **osqhunt(1001) · smonbd(1002) · mw11bot(1003) · insdr12(1004)** — uid 연속 4개.
- **판정**: 시나리오마다 red 미션이 계정을 심는 **표준 절차**. 개별 인시던트로 나열하지 않고 **"지속성:
  계정 생성 패턴(uid 1001~1004, web VM /etc/passwd)"** 하나로 집약(→ 교훈 #14). FIM 이 매번 탐지 중.

## 2. 방어 측 — 결함이 **시스템적** (개별 실수 아님)

이번 20건 grade 의 blue 룰을 보면 거의 **동일한 결함**이 반복된다:

```
대부분의 Suricata blue 룰 = alert http any any -> any any (... http.uri; content:"UNION"; content:"SELECT")
```
- 미션이 무엇을 요구하든(`http.user_agent`, SSRF `169.254.169.254`, drop 액션) **무관하게**
  `alert`+`http.uri;content:UNION;SELECT` 를 **복붙**한 정황. 관련 sid: 9406001·9407001·9408001·9409001·
  9510001·9511001·9512001·9515015 …
- 결과: ① **액션 alert(미차단)**, ② **필드/조건 미스매치(무력 탐지)** — cycle-006 #13 결함이 **규모로** 재현.

> **관제·교육 결론**: 이건 한 학생의 실수가 아니라 **다수가 같은 템플릿을 복붙**한 *시스템적* 결함이다.
> N개의 개별 오탐지가 아니라 **"blue 진영의 룰 작성 역량 갭"** 단일 신호로 보고해야 한다(→ 교훈 #14).

## 3. 주시하던 신호 (아직 미출현)

- 시나리오 55(웹쉘)·56(C2 비콘)·46(sysmon 리버스셸)이 활성이나, **실제 C2 비콘/웹쉘 콜백/리버스셸
  네트워크 시그니처는 SIEM 에 아직 미관측.** 현재 증거는 여전히 WAF/계정/룰 중심. 계속 주시.

---

## 4. 방법론 교훈 (→ v0.3.4)

- **#14 반복되는 동일 결함/행위는 집약하라 — 개별 N건이 아닌 시스템적 신호로.** 같은 결함(복붙 룰)이나
  같은 행위(계정 생성)가 다수 actor/시나리오에 반복되면, N개 인시던트로 나열하면 노이즈가 된다.
  **패턴 1건 + 인스턴스 카운트 + 대표 증거**로 보고한다. (cycle-004 #11 "볼륨≠신호"의 *반복 차원* 확장:
  반복의 **균질성**이 곧 신호 — "다수가 같은 실수"는 시스템/커리큘럼 갭을 가리킨다.)

## 5. 권고

1. **지속성 계정 일괄 점검**: uid 1001~1004(osqhunt/smonbd/mw11bot/insdr12) 전수 무력화 + 생성 경로.
2. **방어 룰 작성 가이드 시급**: alert↔drop, 매칭 필드 선택의 시스템적 오류 → 템플릿 복붙 방지 교육/검수.
3. **INC-003 SSRF 대응**: `169.254.169.254`(메타데이터) egress 차단·탐지 룰 권장.

## 6. 다음 사이클

- `2026-06-10T18:13:37.481Z` 이후. C2/웹쉘/리버스셸 **네트워크 신호** 첫 출현이 최우선 관전 포인트.
- 외부 WAN(10.20.20.x) 여전히 0건.

---

*Generated by AI SOC agent · OpenSearch 델타 파싱(읽기 전용) · 6v6 무접촉 · 방법론 [v0.3.4](../../soc-methodology/README.md)*
</content>
