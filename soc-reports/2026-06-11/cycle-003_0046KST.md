# SOC 관제 리포트 — Cycle 003 (첫 라이브 델타 / Live Delta)

| 항목 | 값 |
|------|-----|
| 생성 시각 | 2026-06-11 00:46 KST |
| 유형 | **LIVE DELTA** — 라이브 신규 이벤트 첫 인입 분석 |
| 델타 윈도우 | 2026-06-10T15:21:43Z ~ 15:43:57Z (KST 00:21~00:43) |
| 신규 이벤트 | 126 (kind: mission_check 91 / fim 25 / grade 7 / alert 3) |
| last_seen 갱신 | **2026-06-10T15:43:57.565Z** (다음 사이클 기준점) |

> 14시간의 조용한 사이클 끝에 첫 라이브 트래픽이 들어왔다. 정찰(cycle 1·2의 nmap) 단계를 넘어
> **웹 공격 → 권한상승/지속성**으로 공격이 진행됐다 — 코호트 전반의 첫 실제 킬체인.

---

## 1. 무슨 일이 있었나 (시나리오 맥락)

| 시나리오 | 제목 | 이벤트 |
|----------|------|--------|
| 43 | [보안운영 W8] 중간고사 — 한 침입 사슬을 5종 장비로 끝까지 막아내기 | 68 |
| 42 | [보안운영 W7] 숨은 침입자 사냥 — osquery로 발판 끄집어내기 | 21 |
| 40 | [보안운영입문 W6] WAF 우회 공격 막기 + 한 공격을 세 장비에서 추적 | 20 |
| 41 | [보안운영 W6] WAF 운영자 포렌식 — anomaly score 차단 + 오탐 수술 | 17 |

액터: 김철수(92) 주력, 신짱구(34). mission_check 67 pass / 24 fail.

---

## 2. 탐지된 위협 · 인시던트 (신규)

### INC-003 — 웹 공격(SQLi/XSS/스캐너) WAF 차단 · 진짜 출처 식별

- **출처**: HAProxy 방화벽 de-NAT 가 원본 client 를 **`10.20.30.202` (insider-ext 공격 발판)** 으로 복원.
  - `alert` rule **100251** lvl 6 "HAProxy(fw): 웹 공격 의심 원본 IP 10.20.30.202 (de-NAT)" — 3건
    (15:21:43 / 15:33:27 / 15:38:37Z).
- **WAF(ModSecurity) 탐지·차단** (grade 증거로 교차확인):
  - SQLi rule **942**, 스캐너/프로토콜 rule **913/941/930**, **anomaly score block 949110**, **HTTP 403** 응답.
  - XSS 시도: `GET /?q=...%3Cscript%3Ealert(...)` (reflected XSS), modsec_audit.log 의 동일 transaction_id 로 추적.
- **판정**: 공격이 타깃 web 에 도달했고 **WAF 가 403 으로 차단**. 차단 성공 — 단, 시도 자체는 실재.

> **귀속 진전(중요)**: cycle-002 에서 "src=.1 은 게이트웨이라 호스트 식별 불가"였는데, **HAProxy de-NAT
> 가 L7 에서 진짜 출처 호스트(10.20.30.202)를 복원**했다. → §4 방법론 교훈 #7.

### INC-004 — 권한상승 / 지속성 계정 생성 (FIM 탐지)

- **FIM(File Integrity Monitoring)** 가 web 컨테이너에서 계정 관련 파일 **동시 변경** 포착 (15:35:59, who=root):
  `/etc/passwd-`, `/etc/shadow(-)`, `/etc/group`, `/etc/gshadow`, `/etc/subuid(-)`, `/etc/subgid` (25 fim 이벤트).
- **백도어 계정**: grade 증거에서 `/etc/passwd` 에 `osqhunt:x:1001:1001::/home/osqhunt:/bin/bash` 실존 확인
  (uid≥1001 일반계정). 시나리오 42(osquery 침입자 헌팅)의 표적 발판.
- **판정**: 공격자가 타깃에 **지속성 계정을 심었고 FIM 이 탐지**. blue 측은 osquery 로 헌팅.

### OBS-001 (지속) — LAN 관리자 양성 트래픽
- HAProxy rule **100250** de-NAT → `192.168.0.107` (LAN admin) 6건. cycle-002 대로 **양성·화이트리스트**.

---

## 3. 공격 진행 (킬체인) — 사이클 누적 관점

```
[cycle 1·2]  정찰(Recon)        nmap SYN 스윕 → DMZ 자산 5개(8000/443/5601)
                 │
[cycle 3]    초기 침투(Web)     SQLi/XSS/스캐너 → ModSecurity WAF 403 차단 (출처 10.20.30.202)
                 │
[cycle 3]    지속성(Persist)    osqhunt 계정 생성 → /etc/passwd·shadow FIM 탐지
                 │
             방어 대응           WAF anomaly 포렌식(41) · osquery 헌팅(42) · 5종장비 종합방어(43)
```

처음으로 **공격(red)과 방어(blue)가 같은 사건을 양쪽에서** 다루는 게 관측됐다 — 진짜 SOC 시나리오.

---

## 4. 이번 사이클의 방법론 교훈 (→ v0.3)

- **#7 L7 de-NAT = 진짜 출처, L3/IDS = 경로.** HAProxy de-NAT 가 호스트 출처(10.20.30.202)를 줬다.
  Suricata 의 게이트웨이-src(#5) 한계를 보완 → **계층별 귀속**: 네트워크층=경로/세그먼트, 앱/WAF층=출처 호스트.
- **#8 새 신호종 등장.** `fim`(파일 무결성)은 권한상승/지속성 1급 신호, HAProxy `alert`(rule 100251)은
  웹공격 1급 신호 → 증거 분류표 확장(§ 방법론 2.1/4.5).
- **#9 코호트 stamp 누락(데이터 품질).** 신규 126건이 전부 `tubewar-activity-identity`(cohort_path 공백)에
  적재됨 → **코호트 드릴다운이 이 배틀들을 못 봄**. ts 기반 델타는 정상 동작하나, 코호트 RBAC/필터는 사각.
- **#10 경보 과소수집 정량화.** grade 증거상 lab Wazuh 엔 Suricata 4485건·HAProxy 113건이 있으나 중앙
  SIEM `alert` 로는 3건만 도달(~0.1%). cycle-001 G-1 을 규모로 확증.

---

## 5. 권고 (real-SOC)

1. **INC-004 즉시 대응 우선순위 최상**: 지속성 계정(osqhunt)은 실침해 지표. 계정 무력화·생성 주체 추적
   (15:35:59 web 컨테이너, who=root → 권한상승 경로 역추적).
2. **INC-003 출처 차단**: 10.20.30.202(insider-ext) 발판을 격리. WAF 가 막았지만 반복 시도 모니터링.
3. **코호트 stamp 수정(G-6/#9)**: 신규 배틀이 cohort 없이 적재되는 원인 점검(코드측, 본 에이전트 미적용).
4. **경보 승격(G-1/#10)**: 0.1% 수집률 — Suricata/Wazuh 경보를 1급 `alert` 로 승격해야 실시간 관제 가능.

---

## 6. 다음 사이클에서 볼 것

- 시나리오 43(중간고사 5종장비 종합방어) 추가 이벤트 — 전체 킬체인 방어 완성도.
- 10.20.30.202 의 후속 행위(lateral·exfil)로 에스컬레이션 여부.
- 외부 순수 WAN(10.20.20.x) 트래픽 — 여전히 0건.
- 코호트 stamp 누락이 지속되는지(데이터 품질 회귀).

---

*Generated by AI SOC agent · OpenSearch 델타 파싱(읽기 전용) · 6v6 무접촉 · 방법론 [v0.3](../../soc-methodology/README.md)*
</content>
