# SOC 관제 리포트 — Cycle 006 (Intel: 기법 확장 · 탐지≠차단)

| 항목 | 값 |
|------|-----|
| 생성 시각 | 2026-06-11 02:43 KST |
| 유형 | **INTEL** — 신규 공격 기법(LFI) + 2차 백도어 계정 + 방어 룰 결함(규모) |
| 델타 윈도우 | 2026-06-10T17:14:00Z ~ 17:43:13Z |
| 신규 이벤트 | 161 (mission_check 129 / grade 19 / alert 13) |
| last_seen 갱신 | **2026-06-10T17:43:13.009Z** |

> 시나리오가 **W11~W15 + SOC관제 W6~W10**(STIX·SIGMA·ATT&CK·IR·웹쉘 포렌식)로 대거 확장됐다.
> 공격은 강도·기법이 늘었고, 방어 룰 결함이 *규모로* 드러나 **"탐지 ≠ 차단"** 이 핵심 교훈으로 부상.

---

## 1. 위협 측 — 기법 확장 + 2차 지속성

### INC-003 확장 — 웹 공격 다변화·강화 (출처 10.20.30.202 지속)
- HAProxy `alert` rule 100251 lvl6 (10.20.30.202, de-NAT) **13건** (17:16~17:41Z) — 빈도 급증.
- grade 증거가 드러낸 **신규 기법**:
  - **LFI(Local File Inclusion)**: WAF rule **930100**, 페이로드 `f=../../../etc/passwd` (sa8r1-kim).
  - **스캐너 다양화**: `nikto/2.1.5` UA → rule **913100** (기존 sqlmap 외 추가).
  - XSS 쿠키 탈취: `<script>alert(document.cookie)</script>` (ir9r1, pass 20/20).
  - 전부 `X-Forwarded-For: 10.20.30.202`(attacker-ext) — 출처 일관.
- **판정**: 출처는 동일하나 **공격 기법이 SQLi/XSS → +LFI +스캐너 다변화**로 확장. INC-003 심각도 유지·확장.

### INC-006 (신규) — 2차 백도어 계정 `smonbd`
- web VM `/etc/passwd` 에 `smonbd:x:1002:1002::/home/smonbd:/bin/bash` 실존(red-2 증거).
- INC-004(`osqhunt` uid 1001)에 이은 **두 번째 지속성 계정**(uid 1002). 지속성 패턴 반복 → 계정 생성이
  공격자의 표준 절차임을 시사.

## 2. 방어 측 — 룰 결함이 규모로: **탐지 ≠ 차단**

다수 blue 가 탐지 룰을 배포했으나 grade 가 드러낸 **체계적 결함 3종**:

| 결함 유형 | 증거 | 영향 |
|-----------|------|------|
| **액션 오류 (탐지만, 차단 안 함)** | Suricata `sid 9509001` action=`alert` (요구: `drop`) → 봉쇄미션 4/20 | **미차단** — 경보만 뜨고 공격 통과 |
| **매칭 필드 오류** | `sid 9507001/9515001` `http.uri; content:UNION` (요구: `http.user_agent`) → 6/16 | **무력 탐지** — 의도한 트래픽 안 잡힘 |
| **내용/스펙 불일치** | Wazuh `id 101201/101301/101401` match=sqlmap 존재하나 요구 내용과 다름 | 형식만 합격, 실효 의문 |

> **핵심**: 룰이 *존재*하고 *로드*돼도(exit_code 0), **액션이 `alert`면 차단되지 않고**, **매칭 필드가
> 틀리면 해당 트래픽을 못 잡는다.** 학생들이 "탐지 룰을 만들었다"는 사실과 "실제로 막힌다"는 별개다.

## 3. 새 신호종 · 연습 진행

- Wazuh **rule 5502 "PAM: Login session closed"** 1건 — 인증/세션 신호 첫 등장(향후 로그인 이상 추적 단서).
- 연습 트랙 확장: **위협인텔(47 STIX·48 enrichment·49 헌팅·50 APT 기말)**, **SOC관제(51 ATT&CK·52 SIGMA
  이식·53 교차분석·54 IR 60분·55 웹쉘 포렌식)**. → 방어가 *룰 작성→상관→헌팅→IR* 로 성숙.

---

## 4. 방법론 교훈 (→ v0.3.3)

- **#13 탐지 ≠ 차단 — 룰의 ACTION·FIELD 를 평가하라.** #12(룰 품질)를 구체화: 룰 *존재*만으로 방어
  효능을 인정하면 안 된다. (a) **액션**(alert=탐지 vs drop/deny=차단), (b) **매칭 필드/조건**(올바른
  필드인가), (c) **로드 여부** 세 축으로 본다. "봉쇄(contain)"는 drop 액션이 있어야 성립.
- (운영) 공격자의 **계정 생성**이 반복 절차로 확인됨(osqhunt→smonbd) → FIM `/etc/passwd·shadow` 변경을
  **지속성 1순위 헌팅 쿼리**로 상시화.

---

## 5. 권고

1. **INC-006 즉시 대응**: smonbd(uid 1002) 계정 무력화 + osqhunt(INC-004) 재확인. 계정 생성 경로 역추적.
2. **방어 룰 교정 우선순위**: `sid 9509001` (alert→drop) — 탐지는 되나 *차단 안 됨*이 가장 위험.
   매칭 필드 오류(9507001/9515001) 다음.
3. **INC-003 LFI 대응**: `../../../etc/passwd` 경로순회 차단 룰·민감파일 접근 모니터 추가 권장.
4. 외부 WAN(10.20.20.x) 여전히 0건 — 진짜 outsider 시나리오 미수행 추정.

## 6. 다음 사이클

- `2026-06-10T17:43:13.009Z` 이후. 시나리오 46(sysmon) **리버스셸/인코딩 명령** 및 55(웹쉘) **콜백**
  신호 — 새 위협종(C2/웹쉘) 첫 출현 주시.

---

*Generated by AI SOC agent · OpenSearch 델타 파싱(읽기 전용) · 6v6 무접촉 · 방법론 [v0.3.3](../../soc-methodology/README.md)*
</content>
